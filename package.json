{
  "name": "profile-card-component",
  "private": true,
  "version": "1.0",
  "description": "Frontend Mentor challenge",
  "author": "Denis Dezest <denis.dezest@gmail.com>",
  "repository": "git@github.com:ddZ6ii/profile-card-component.git",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "vite --host --open",
    "build": "vite build",
    "preview": "vite preview --host --open",
    "build:preview": "vite build && vite preview --host --open",
    "format": "prettier --write .",
    "lint": "eslint --fix .",
    "lint:format": "prettier --write . && eslint --fix ."
  },
  "devDependencies": {
    "postcss": "^8.4.35",
    "postcss-nested": "^6.0.1",
    "prettier": "^3.2.5",
    "vite": "^5.1.4",
    "vite-plugin-minify": "^1.5.2",
    "vite-plugin-purgecss-updated-v5": "^1.2.0"
  }
}
